## 之前周五手指手术，歇了几天

## vscode+jlink快捷烧录（烧录前编译代码）
### tasks.json(以f103为例，其他类型仅需更改芯片即可)
```
{
  "version": "2.0.0",
  "tasks": [
    {
        "label": "Build",
        "type": "shell",
        "command": "cmake --build ${workspaceFolder}/build/Debug",
        "problemMatcher": []
    },
    {
      "label": "Flash STM32 via J-Link",
      "type": "process",
      "command": "/opt/SEGGER/JLink/JLinkExe",
      "args": [
        "-Device", "STM32F103C8",
        "-If", "SWD",
        "-Speed", "4000",
        "-CommanderScript", "${workspaceFolder}/flash.jlink"
      ],
      "group": { "kind": "build", "isDefault": true },
      "problemMatcher": [],
      "dependsOn": "Build"
    }
  ]
}
```

### flash.jlink(以6_LEDpc13_LinuxCmakeTest为例)
```
r
h
loadfile build/Debug/6_LEDpc13_LinuxCmakeTest.elf
r
q
```

## 将claude.md拓展，在工程文件夹下加入编译报错、ai总结等等内容，避免聊天框输入/反复强调，trae.md

## #Tools/git/修改上一次的commit  
Linux丨修改了MCLM,测试了bmp280,mpu6050,增加一键烧录之前的编译

## 增加了goal.md(目标期望),result.md(结果，编译报错等),trae.md(项目文档，接口文档，流梳理)，
### 第一次实现比较规范的开发流程


### 在linux部署了vofa+，并打印了mpu6050的绘图
#### 6_Mpu6050_t1
![[Pasted image 20260407212826.png]]


