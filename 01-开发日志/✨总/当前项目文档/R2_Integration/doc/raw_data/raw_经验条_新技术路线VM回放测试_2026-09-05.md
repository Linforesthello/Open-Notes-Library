# 暂存经验条（待本任务完成后独立成文档）

> 来源：R2 会话 2026-09-05（用户口述，原文要点）
> 状态：**暂存（/tmp），任务完成后独立成文**——「仿照现有其他经验进行落档」（retrospect 事件层或 analysis-methods draft 层，形态待定）
> 注意：/tmp 内容可能随重启丢失，独立文档完成后本文件即无保留价值

---

## 经验主题：新技术路线的应用/部署/替代——测试流程

**用户原话要点**：
「不是之前已经测过了么，scan 看不到低矮物体仅 +1 度，且并没有发生任何更改，所以结论仍然是上一次的。
我想的是先在 vmware 完成部署，测试就用原先录制好的数据进行重新测试，这个可行么？
（这是否又是一个经验？关于新技术路线的应用、部署和替代的测试流程，
先把这一条要求保存在本地 /tmp 之类的暂存，等这边做完后再独立文档，仿照现有的其他经验进行落档）」

**提取的经验主张（待验证/成型）**：
1. 旧结论不因"方案一直没实施"而失效：感知断点结论（/scan 单环 +1°、低环数据在 points 里）成立，
   修法落地前无需重测旧结论本身——先 VM 部署 + 旧 bag 回放验证**新配置的效果**，再谈实车。
2. 新技术路线（新配置/新层/新包）在真实硬件窗口稀缺时：
   **先在开发机（VM）完成部署与构建链路 → 用既有录制数据（bag）回放做回归验证 → 通过后再上实车**。
   - 前提条件：bag 里必须录有所需话题（本次即 /velodyne_points；W3 三块漏录 points 即不可回放）；
     场景需含被测物；帧/tf 完整。
   - 限制：bag 回放只能验证"感知/代价地图层行为"，CPU 消耗、controller 闭环行为、参数微调仍需实车。
3. （推断，待任务后复核）验证分层：断点修复类改动 → 回放级验证即可收口；行为/性能类改动 → 回放不够。

**本任务上下文**：R2 低物盲区修法候选 ②"costmap 直吃 points 低带"（obstacle/voxel layer + 高度带）实现中，
用户决策：只改 nav2_params_low.yaml，先 VM（安装链路 + bag 回放重测），N97 部署待 VM 验证后。
